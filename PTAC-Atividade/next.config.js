/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "www.ifms.edu.br",
            "www.moodle.org",
            "usagif.com"
        ]
    }
}

module.exports = nextConfig
